import React from 'react'
import ReactDOM from 'react-dom/client'
import CarmonaOS from './CarmonaOS.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CarmonaOS />
  </React.StrictMode>,
)
